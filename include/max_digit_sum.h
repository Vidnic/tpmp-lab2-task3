/* max_digit_sum.h */
/* Author: Видук Павел, group 10 */

#ifndef MAX_DIGIT_SUM_H
#define MAX_DIGIT_SUM_H

#include <stddef.h>

int digit_sum(int n);
size_t find_numbers_with_max_sum(const int *arr, size_t size, int *result, size_t result_size);

#endif
